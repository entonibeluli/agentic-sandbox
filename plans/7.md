# Plan for #7: Add readiness and liveness probes, run two replicas

## Goal
Configure the Kubernetes Deployment with two replicas plus readiness and liveness probes against the existing `/health` endpoint on port 3000. Keep application code, API behavior, Service ports, environment variables, and browser behavior unchanged.

## Dependencies (confirmed / plausible / open), each with change: yes | no
- confirmed: `k8s/deployment.yaml` — currently has `replicas: 1`, the HTTP container port, and no probes; this is the implementation surface. change: yes
- confirmed: `src/app.ts` — `GET /health` already returns a successful JSON response suitable for both probes. change: no
- confirmed: `k8s/service.yaml` — routes the named `http` port to the application; probe configuration does not require a Service change. change: no
- confirmed: `src/config.ts`, `k8s/configmap.yaml` — no new environment variable is needed. change: no
- confirmed: `test/app.test.ts`, `e2e/smoke.spec.ts` — health coverage already exists; no new test file is required for manifest-only behavior. change: no
- confirmed: `README.md` — no API or environment contract changes are required; deployment documentation is not currently specific about replica/probe settings. change: no
- plausible: `.github/workflows/ci.yml` — existing rollout, stability, smoke, and browser gates should validate the manifest; verify through the pipeline without editing workflow code. change: no
- open: none; the ticket specifies the replica count and probe endpoint.

## Tests
- Existing `/health` unit and browser smoke tests remain unchanged.
- Pipeline deployment checks must confirm rollout completion, both pods Ready, no restarts during stability, smoke success, and browser success.
- No local Kubernetes verification is available; manifest behavior is pipeline-only.

## Environment contract
No variables are added or changed. `src/config.ts`, `k8s/configmap.yaml`, and the README environment table remain synchronized.

## Risks
- Incorrect probe path, port, or timing could prevent rollout even though the process is healthy.
- Two replicas increase resource usage; preserve the existing requests and limits.
- Probe failures could cause restarts or route traffic before readiness, directly affecting rollout and stability gates.
- Smoke and browser checks may expose readiness timing issues after scaling.

## Open questions
None.

## Decisions
- Use `/health` on the named `http` port (container port 3000) for both probes.
- Set `replicas: 2`.
- No application, Service, ConfigMap, README, or CI workflow changes are required.
- Implemented in `k8s/deployment.yaml`.
